import { Router } from 'express';

const router = Router();

// Placeholder routes untuk registration
router.get('/', (_req, res) => {
  res.json({ message: 'Registration routes are working!' });
});

export default router;